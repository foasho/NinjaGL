import React, { useState } from 'react';

/**
 * - Sceneが変更されるときに表示されるローダー
 */

export const NinjaLoader = () => {

  const [loading, setLoading] = useState(true);
  const [fadeOut, setFadeOut] = useState(false);

  return (
    <>
    </>
  )
};