services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    image: ninjagl-editor
    container_name: ninjagl-editor
    ports:
      - '3001:3001'
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./app:/app/app
      - ./lib:/app/lib
      - node_modules:/app/node_modules
    restart: always
    tty: true
    stdin_open: true

volumes:
  node_modules:
