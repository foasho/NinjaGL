import { useContext } from "react"
import { NaniwaEditorContext } from "../NaniwaEditorManager"


export const DebugPlay = () => {
    const editor = useContext(NaniwaEditorContext);
    /**
     * OMとUIをNaniwaEngineを動作させる
     */
    return (
        <>
        </>
    )
}